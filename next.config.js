/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'images.unsplash.com', 'plus.unsplash.com', 'picsum.photos', 'media.istockphoto.com'],
  },
}

module.exports = nextConfig
